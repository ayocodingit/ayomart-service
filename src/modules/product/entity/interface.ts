export type Store = {
    code: string
    name: string
    unit: string
    category: string
    price: number
    grosir_price: number
    stock: number
    discount: number
    description: string
    images: string[]
}
